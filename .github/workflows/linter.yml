name: Linter

on:
  push:
    branches: [ main ]
  pull_request:
      types: [ opened ]
      branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - name: Super linter
        uses: docker://github/super-linter:v2.1.0
